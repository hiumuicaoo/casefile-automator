# ===== Tailscale sidecar =====
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxx
TS_HOSTNAME=giamdinh

# Ghi chú: DATABASE_URL đã hard-code trong docker-compose.yml (postgres nội bộ).
# Không cần biến Supabase — dashboard dùng Postgres local trên server.
